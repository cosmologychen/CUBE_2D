# Project State: CUBE_2D Lightcone Refactor

## 关键事实纠正 (Knowledge Correction)
- **PID 定义**：在本项目（2D 版本）中，粒子标识 `pid(ip)` 始终等于其数组索引 `ip`。
- **设计初衷**：程序保留 `pid` 数组及其相关的 `_pid.bin` 输出，主要是为了方便未来移植到真实的三维大规模模拟。
- **实现要求**：尽管目前 `pid == ip`，所有逻辑仍必须通过 `pid` 数组操作以维持架构一致性。

## 进度记录 (Progress Log)
### 2026-04-27
- **模拟运行状态 [CURRENT]**：
    1. **org**: 原始模拟已完成运行。
    2. **lcic**: 光锥初始条件模拟已完成。
    3. **runtime**: 原始模拟实时保存光锥版本已完成运行，已生成 `runtime_lightcone_*.bin`。
- **下一步计划 [NEXT]**：
    - 对比 **切片光锥 (slice lightcone)** 与 **实时保存光锥 (runtime lightcone)** 的物理一致性。
    - **待修复**：`utilities/test_lc_geom.f90` 目前存在问题，需在后续对话中调试并修正以完成比对分析。

### 2026-04-26
- **代码重构 [ALGORITHM ALIGNED]**：
    - **忠实对齐**：重新实现 `runtime_lightcone.f90`，严格遵循 `RunTime——lc.md` 的二阶 Newton-Raphson 迭代公式。
    - **流式 I/O [SAFETY]**：实现每一步刷新 (`flush`) 机制，修正了 `position='asiso'` 的 typo 为 `'rewind'`。
    - **性能计时**：集成 `tic(15)` 计时，修正了打印格式及 `lc_file_a` 相关的冗余逻辑。
- **后处理工具 (`lightcone.f90`) [VERIFIED]**：
    - **密度汇总 [SUCCESS]**：成功生成 $2048^2$ 光锥密度场。
- **验证测试 [SUCCESS]**：
    - 验证结果显示 `test_lc_geom` 的几何计算逻辑在合成数据上是正确的。

## 待办事项 (TODO)
- [x] 忠实对齐 `RunTime——lc.md` 算法并加入计时与流式刷新。
- [x] 清理 `runtime_lightcone.f90` 中冗余的 `lc_file_a` 逻辑。
- [ ] **调试工具**：修正 `utilities/test_lc_geom.f90`，使其能正确比对 org 和 runtime 的输出。
- [ ] **物理比对**：运行比对脚本，生成 `lc_comparison_results.txt` 并分析残差。
- [ ] **未来计划**：重构 `runtime_lightcone` 为独立异步线程运行。
